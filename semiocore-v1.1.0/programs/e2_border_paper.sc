# SemioCore v1.0 — Paper-grade E2: long trace + richer context
seed 12345;
context Add(0.5) >> Sign >> JitterU(0.05) {
  tick 1.0;
  u1 := sense chN;
  commit u1;

  tick 1.0;
  u2 := sense chN;
  commit u2;

  tick 1.0;
  u3 := sense chN;
  commit u3;

  tick 1.0;
  u4 := sense chN;
  commit u4;

  tick 1.0;
  u5 := sense chN;
  commit u5;

  tick 1.0;
  u6 := sense chN;
  commit u6;

  tick 1.0;
  u7 := sense chN;
  commit u7;

  tick 1.0;
  u8 := sense chN;
  commit u8;

  tick 1.0;
  u9 := sense chN;
  commit u9;

  tick 1.0;
  u10 := sense chN;
  commit u10;

  tick 1.0;
  u11 := sense chN;
  commit u11;

  tick 1.0;
  u12 := sense chN;
  commit u12;

  tick 1.0;
  u13 := sense chN;
  commit u13;

  tick 1.0;
  u14 := sense chN;
  commit u14;

  tick 1.0;
  u15 := sense chN;
  commit u15;

  tick 1.0;
  u16 := sense chN;
  commit u16;

  tick 1.0;
  u17 := sense chN;
  commit u17;

  tick 1.0;
  u18 := sense chN;
  commit u18;

  tick 1.0;
  u19 := sense chN;
  commit u19;

  tick 1.0;
  u20 := sense chN;
  commit u20;

  tick 1.0;
  u21 := sense chN;
  commit u21;

  tick 1.0;
  u22 := sense chN;
  commit u22;

  tick 1.0;
  u23 := sense chN;
  commit u23;

  tick 1.0;
  u24 := sense chN;
  commit u24;

  tick 1.0;
  u25 := sense chN;
  commit u25;

  tick 1.0;
  u26 := sense chN;
  commit u26;

  tick 1.0;
  u27 := sense chN;
  commit u27;

  tick 1.0;
  u28 := sense chN;
  commit u28;

  tick 1.0;
  u29 := sense chN;
  commit u29;

  tick 1.0;
  u30 := sense chN;
  commit u30;

  tick 1.0;
  u31 := sense chN;
  commit u31;

  tick 1.0;
  u32 := sense chN;
  commit u32;

  tick 1.0;
  u33 := sense chN;
  commit u33;

  tick 1.0;
  u34 := sense chN;
  commit u34;

  tick 1.0;
  u35 := sense chN;
  commit u35;

  tick 1.0;
  u36 := sense chN;
  commit u36;

  tick 1.0;
  u37 := sense chN;
  commit u37;

  tick 1.0;
  u38 := sense chN;
  commit u38;

  tick 1.0;
  u39 := sense chN;
  commit u39;

  tick 1.0;
  u40 := sense chN;
  commit u40;

  tick 1.0;
  u41 := sense chN;
  commit u41;

  tick 1.0;
  u42 := sense chN;
  commit u42;

  tick 1.0;
  u43 := sense chN;
  commit u43;

  tick 1.0;
  u44 := sense chN;
  commit u44;

  tick 1.0;
  u45 := sense chN;
  commit u45;

  tick 1.0;
  u46 := sense chN;
  commit u46;

  tick 1.0;
  u47 := sense chN;
  commit u47;

  tick 1.0;
  u48 := sense chN;
  commit u48;

  tick 1.0;
  u49 := sense chN;
  commit u49;

  tick 1.0;
  u50 := sense chN;
  commit u50;

}
out := summarize;